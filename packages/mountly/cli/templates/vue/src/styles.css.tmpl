{{tailwindImport}}@source "./";

:host {
  display: block;
  padding: 1rem;
}

.ui-card {
  color: var(--ui-text);
  background: var(--ui-surface);
  border-radius: var(--ui-radius);
  padding: 1rem;
}
