// Simulates a CSS-Modules-style import: the build emits a `styles` object
// whose values are the hashed class names found in the sibling .css file.
export const styles = {
  button: "button_h_4f9c1",
  label: "label_h_4f9c1",
};
