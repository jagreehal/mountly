---
title: MCP Apps
description: Build React, Vue, or Svelte Views for MCP Apps and install them on the server you already run.
---

You need interactive UI next to an MCP tool result. [MCP Apps](https://github.com/modelcontextprotocol/ext-apps)
(SEP-1865) is the protocol. `mountly-mcp` is the View kit: component, `ui://`
artifact, local host, then `registerMcpApps` on your server.

```bash
npx mountly-mcp create my-app --framework react
cd my-app && pnpm install && pnpm dev
```

That opens a sandboxed AppBridge host against your `server.mjs`. Point Claude
Desktop or VS Code at `serve-stdio.mjs` when you leave the laptop preview.
[Host matrix](/mcp-apps/host-matrix/).

## Agent Skills

| Skill                                                                                                                       | Ask for                             |
| --------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| [`create-mcp-app`](https://github.com/jagreehal/mountly/blob/main/plugins/mountly-mcp/skills/create-mcp-app/SKILL.md)       | "Create an MCP App"                 |
| [`add-app-to-server`](https://github.com/jagreehal/mountly/blob/main/plugins/mountly-mcp/skills/add-app-to-server/SKILL.md) | "Add UI to my MCP server"           |
| [`convert-web-app`](https://github.com/jagreehal/mountly/blob/main/plugins/mountly-mcp/skills/convert-web-app/SKILL.md)     | "Turn my component into an MCP App" |
| [`migrate-ext-apps`](https://github.com/jagreehal/mountly/blob/main/plugins/mountly-mcp/skills/migrate-ext-apps/SKILL.md)   | "Migrate from ext-apps"             |

Install steps: [Agent Skills](/mcp-apps/agent-skills/).

```ts
import { createMcpView } from "mountly-mcp/react";
import Dashboard from "./Dashboard";

createMcpView(Dashboard);
```

`mountly-mcp build` writes HTML plus `dist/mountly-mcp.manifest.json`.
`registerMcpApps()` loads that manifest into an unconnected `McpServer`.

## Pick a path

| You have                         | You run                                                    |
| -------------------------------- | ---------------------------------------------------------- |
| Empty folder                     | `mountly-mcp create`                                       |
| Existing React / Vue / Svelte UI | `createMcpView` + Vite                                     |
| Second View in the same project  | `mountly-mcp add`                                          |
| Production MCP server            | `registerMcpApps()` before `connect`                       |
| Host or bridge protocol work     | [`@modelcontextprotocol/ext-apps`](/mcp-apps/vs-ext-apps/) |

You keep auth, transports, prompts, ordinary tools, and deploy. Mountly owns
View build, bridge HTML, and registration. The wire protocol stays on
[`@modelcontextprotocol/ext-apps`](https://www.npmjs.com/package/@modelcontextprotocol/ext-apps).

## Install

```bash
# React (default). mountly + mountly-react come with mountly-mcp.
npm install mountly-mcp react react-dom
npm install @modelcontextprotocol/sdk   # if you import McpServer
npm install -D vite @vitejs/plugin-react

# Vue or Svelte
npm install mountly-mcp mountly-vue vue   # or mountly-svelte + svelte
```

Floor: Node 20+, React 19, Vite 8. Check with
`npx mountly-mcp doctor`.

## Modules

| Module                                       | Job                                   |
| -------------------------------------------- | ------------------------------------- |
| `mountly-mcp/react` (also `/vue`, `/svelte`) | Wrap a component; expose tool data    |
| `mountly-mcp/vite` + `/artifact`             | Emit and read App artifacts           |
| `mountly-mcp/server`                         | `registerMcpApps` on your `McpServer` |
| CLI `dev` / `verify`                         | Local AppBridge host and CI checks    |

## Docs map

| Page                                                      | Use when                                      |
| --------------------------------------------------------- | --------------------------------------------- |
| [vs ext-apps](/mcp-apps/vs-ext-apps/)                     | You wonder if you need the official SDK alone |
| [Quick start](/mcp-apps/quick-start/)                     | You wire Vite by hand                         |
| [Host matrix](/mcp-apps/host-matrix/)                     | You connect Claude, VS Code, or ChatGPT       |
| [Examples](/mcp-apps/examples/)                           | You want copy-paste shapes                    |
| [Build](/mcp-apps/build-and-artifacts/)                   | Multi-View manifests                          |
| [Production](/mcp-apps/production-integration/)           | Existing server install                       |
| [Dev and verify](/mcp-apps/development-and-verification/) | `dev`, `doctor`, CI                           |
| [How it works](/mcp-apps/how-it-works/)                   | Lifecycle and text-only hosts                 |

Catalog-driven generative UI lives under
[Generative UI](/concepts/generative-ui/). Skip it until `createMcpView` is
boring.
