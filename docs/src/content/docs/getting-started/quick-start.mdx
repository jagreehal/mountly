---
title: Quick start
description: Scaffold a mountly widget and mount it on a page in 60 seconds.
---

import LiveExample from "../../../components/LiveExample.astro";
import { exampleUrl } from "../../../lib/example-urls.ts";

This is the **runtime** track: a page you run adopts mountly and loads widgets on intent.

:::tip[Handing a component to another team instead?]
You want the **embeds** track, not this one. [Script-tag
embeds](/mountly/concepts/script-embeds/) publish a component as a custom element
the consuming page drops in with one `<script>`. No scaffold, no bundle choice,
and nothing for them to install. [Embeds or
runtime?](/mountly/getting-started/embeds-or-runtime/) settles which you need.
:::

The CLI scaffolds a React widget wired to the build pipeline, both bundle outputs, and a working host page.

[Choosing an architecture](/mountly/getting-started/choosing-an-architecture/) compares a monorepo npm library, mountly runtime composition, and broader MFE platforms.

## 1. Scaffold a widget

```bash
npx mountly init my-widget
cd my-widget
```

You get:

- `src/Component.tsx`: a React component (you can swap in Vue or Svelte later).
- A widget factory using `mountly-react`'s `createWidget`.
- A `tsup` build that produces both `dist/index.js` (self-contained) and `dist/peer.js` (shared React).
- An example host page.

## 2. Install and build

```bash
pnpm install
pnpm build
```

Two artifacts land in `dist/`:

- **`index.js`**: self-contained bundle. Includes React. Drop into any page.
- **`peer.js`**: peer build. Excludes React. Use when the host already maps React via an import map.

See [Distribution](/mountly/concepts/distribution/) for which one to ship.

## 3. Mount it

```html title="host.html"
<div id="mount"></div>
<script type="module">
  import widget from "./my-widget/dist/index.js";
  widget.mount(document.getElementById("mount"));
</script>
```

The widget mounts inside the container in light DOM and the host's CSS reaches in. For full style isolation, pass `shadow: true` to `createWidget`.

## 3b. Lowest-ceremony host (recommended for plain HTML)

On a static or CMS host, prefer declarative islands plus one script tag:

```html title="host.html"
<div data-mountly="/widgets/signup-card.js" data-on="idle" data-props='{"plan":"pro"}'>
  <a href="/signup">Sign up (fallback)</a>
</div>

<script type="module" src="/packages/mountly/dist/auto.js"></script>
```

This is the default DX path: no host app code, one script tag, and fallback HTML that still works when JS is unavailable.

## 4. Add the on-demand lifecycle

`mount()` is the bare minimum. For **on-intent loading**, wrap the widget in a feature:

```ts title="page.ts"
import { createOnDemandFeature } from "mountly/feature";

const signup = createOnDemandFeature({
  moduleId: "signup-card",
  moduleUrl: "./my-widget/dist/index.js",
});

signup.attach({
  trigger: document.querySelector("#cta")!,
  preloadOn: "hover",
  activateOn: "click",
});
```

mountly synthesises the loader and a default render that threads `moduleUrl` through to the adapter, so it fetches and applies the sibling `dist/index.css` for you. It fetches the widget code on first hover and mounts it on click. Open DevTools → Network and watch.

For bespoke loading or rendering, `loadModule` and `render` are still available and override the auto-derived defaults.

## 5. See the full demo

Try the hosted demos without cloning anything:

- <a href={exampleUrl("quickstart/host.html")}>**Quickstart host**</a>: hover to preload, click to
  mount.
- <a href={exampleUrl("marketing-site/")}>**Marketing site**</a>: hero button plus a viewport embed.

<LiveExample path="quickstart/host.html" title="Quickstart" height="300px" />

The marketing site shows hover preload, click mount, and viewport-triggered embeds with zero host framework JS before interaction.

**Run locally** (optional):

```bash
git clone https://github.com/jagreehal/mountly
cd mountly
pnpm install && pnpm -r build
cd docs/examples/plain-html && pnpm dev
```

Then open [http://localhost:5175/docs/examples/quickstart/host.html](http://localhost:5175/docs/examples/quickstart/host.html).

## Where to next

- [**How it works**](/mountly/getting-started/how-it-works/): the architecture in one page.
- [**Triggers**](/mountly/concepts/triggers/): the six ways to say "now".
- [**React** / **Vue** / **Svelte**](/mountly/frameworks/react/): adapter-specific patterns.
- [**API: `createOnDemandFeature`**](/mountly/api/create-on-demand-feature/): the core primitive.
